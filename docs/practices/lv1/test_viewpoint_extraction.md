# テスト観点抽出（汎用）

## 【背景】なぜこのフローが必要なのか

テスト設計の現場では、観点の抽出が担当者の経験や勘に依存しており、属人化と抜け漏れが慢性的な課題となっています。
プロジェクトが変わるたびにゼロから観点を考え直し、過去の障害で得たはずの知見も次の機能に活かせていない状態が続きます。

### AI活用による課題解決のアプローチ

本カリキュラムでは、**「人の方針定義 × AIの網羅的展開 × 人の精査・資産化」** という協働フローで、再利用可能なテスト観点カタログを構築します。

1. **属人化の解消**: ドメイン知識と過去障害をAIへのインプットとして構造化し、誰でも同じ水準で観点を引き出せるようにする。
2. **網羅性の向上**: AIが正常系・異常系・境界値・品質特性など多角的な観点を一括展開し、人間の見落としを補う。
3. **ナレッジの資産化**: 精査・整理した観点をカタログとして蓄積し、同種機能や次のプロジェクトで再利用できる状態にする。

## フローの概要

| AIへのインプット | AIからアウトプット |
| :--- | :--- |
| 要件定義書<br>画面仕様書<br>画面一覧・遷移図<br>機能一覧表<br>過去プロジェクトデータ | 汎用テスト観点表 |

### 協働フロー

1. 👤 **ユーザー (Human)** — **観点抽出方針定義**
    *   テストの対象ドメイン（例：決済、認証、在庫管理）を決定します。
    *   観点の抽象度（再利用前提か、具体機能前提か）と対象レイヤー（業務 / 機能 / 非機能）を定義します。
    *   参照する過去障害の範囲と、品質特性軸（性能・可用性など）を含めるかを判断します。
    *   これらをまとめた**「観点抽出前提定義書」**をAIへのインプットとして用意します。

2. 🤖 **生成AI (AI)** — **観点展開**
    *   前提定義書をもとに、ドメイン共通観点を洗い出します。
    *   正常系・異常系・境界値の観点を網羅的に展開します。
    *   品質特性（性能・可用性・セキュリティなど）別の観点を追加します。
    *   過去障害の情報から再発防止観点を生成し、想定される抜け漏れも提示します。
    *   *目的: 人間が見落としがちな観点を網羅的に広げ、精査の土台を作るため*

3. 👤 **ユーザー (Human)** — **精査・資産化**
    *   AIが展開した汎用観点ドラフト（Draft v0.1）を精査します。
    *   確認ポイント：抽象度の統一・特定プロジェクト依存の排除・再利用可能か・過剰観点になっていないか・品質特性が偏っていないか。

4. 👤 **ユーザー (Human)** — **再利用テンプレ登録**
    *   精査済みの観点を、ドメイン別・品質特性別・レイヤー別などでカテゴリ分類します。
    *   `#決済` `#二重実行` `#非同期処理` などのタグを付けて検索可能な状態にします。
    *   必須 / 推奨 / 条件付き の再利用ルールと、更新ポリシー（障害発生時・年1回など）を定義します。
    *   *目的: 観点を「資産」として蓄積し、次のプロジェクトで即座に活用できるようにする*

## 汎用観点テンプレート（サンプル）

以下は決済ドメインを例に、観点カタログの記述形式を示したサンプルです。

### 業務フロー系観点

| 観点ID | 観点内容 | 適用条件 | 品質特性 | 必須/推奨 |
| :--- | :--- | :--- | :--- | :--- |
| PAY-BIZ-01 | 決済成功時に注文ステータスが「支払済」になる | 全決済機能 | 機能適合性 | 必須 |
| PAY-BIZ-02 | 決済失敗時に注文ステータスが変更されない | 外部決済連携あり | 信頼性 | 必須 |
| PAY-BIZ-03 | 二重決済防止（同一注文IDの再実行制御） | 再送可能設計 | 信頼性 | 必須 |
| PAY-BIZ-04 | 決済後の在庫引当が実行される | 在庫連携あり | 整合性 | 推奨 |
| PAY-BIZ-05 | キャンセル時に返金処理が実行される | 返金機能あり | 業務整合性 | 条件付き |

### 異常系・境界値観点

| 観点ID | 観点内容 | 想定リスク | 必須/推奨 |
| :--- | :--- | :--- | :--- |
| PAY-ERR-01 | 外部決済APIタイムアウト時の挙動 | サービス停止 | 必須 |
| PAY-ERR-02 | 外部API 500エラー時のリトライ制御 | 二重処理 | 必須 |
| PAY-ERR-03 | 不正カード情報入力時のエラーハンドリング | セキュリティ | 必須 |
| PAY-ERR-04 | 通信断発生時のロールバック処理 | データ不整合 | 必須 |
| PAY-ERR-05 | 金額不一致時の処理停止 | 金銭事故 | 必須 |

### データ整合性観点

| 観点ID | 観点内容 | 品質特性 | 必須/推奨 |
| :--- | :--- | :--- | :--- |
| PAY-DATA-01 | 決済成功時に注文テーブルと決済テーブルが一致 | 整合性 | 必須 |
| PAY-DATA-02 | 決済失敗時にDB更新が行われない | 整合性 | 必須 |
| PAY-DATA-03 | ロールバック時に全関連テーブルが元に戻る | 信頼性 | 必須 |
| PAY-DATA-04 | 金額端数処理の丸め誤差確認 | 正確性 | 推奨 |

### 非同期処理系観点

| 観点ID | 観点内容 | 想定リスク | 必須/推奨 |
| :--- | :--- | :--- | :--- |
| PAY-ASYNC-01 | 決済完了通知遅延時の状態管理 | 状態不整合 | 必須 |
| PAY-ASYNC-02 | Webhook再送時の冪等性確認 | 二重登録 | 必須 |
| PAY-ASYNC-03 | 非同期失敗時のリカバリ処理 | データ欠損 | 推奨 |

### 性能・負荷観点

| 観点ID | 観点内容 | 品質目標例 | 必須/推奨 |
| :--- | :--- | :--- | :--- |
| PAY-PERF-01 | 決済APIレスポンス3秒以内 | SLA:3秒以内 | 必須 |
| PAY-PERF-02 | 同時100リクエスト時の安定性 | 同時処理100件 | 推奨 |
| PAY-PERF-03 | 高負荷時のタイムアウト率確認 | エラー率1%未満 | 推奨 |

### 過去障害系観点

| 観点ID | 観点内容 | 発生障害 | 必須/推奨 |
| :--- | :--- | :--- | :--- |
| PAY-INC-01 | 二重送信時の冪等性確認 | 過去二重決済事故 | 必須 |
| PAY-INC-02 | 外部API仕様変更時の互換性確認 | 仕様変更で決済停止 | 推奨 |
| PAY-INC-03 | ログ未出力による調査困難防止 | 障害解析不能 | 推奨 |
